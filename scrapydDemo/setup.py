#coding=utf-8
# Automatically created by: scrapyd-deploy

from setuptools import setup, find_packages
setup(
    name         = 'scrapydDemo',
    version      = '1443188926',
    packages     = find_packages(),
    entry_points = {'scrapy': ['settings = scrapydDemo.settings']},
)
